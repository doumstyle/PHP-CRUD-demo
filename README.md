Create a "db.php" file one directory above these files with :
  
  <pre>
    <?php
      $conn = new msqli("HOST", "DB_USER", "DB_PASS", "DB_NAME");    // Replace these values with your own!
    ?>
  </pre>
